# Autoencoder NIR光谱降维系统 - 使用指南

## 目录

1. [系统要求](#系统要求)
2. [环境安装](#环境安装)
   - [方法一：使用Conda（推荐）](#方法一使用conda推荐)
   - [方法二：使用系统Python](#方法二使用系统python)
3. [项目使用](#项目使用)
4. [配置说明](#配置说明)
5. [常见问题](#常见问题)
6. [错误排查](#错误排查)

---

## 系统要求

### 硬件要求
- **CPU**: 任意现代处理器
- **内存**: 建议 ≥4GB
- **硬盘**: 约300MB可用空间
- **GPU**: 可选（默认使用CPU，兼容性最佳）

### 软件要求
- **操作系统**: Windows 10/11（本指南基于Windows）
- **Python版本**: 3.8、3.9 或 3.10
- **网络**: 需要网络连接下载依赖包

---

## 环境安装

### 方法一：使用Conda（推荐）

Conda可以创建独立的Python环境，避免与系统其他项目冲突。

#### 1. 安装Anaconda或Miniconda

如果您还没有安装Conda：

- **Anaconda**（完整版，约3GB）: [https://www.anaconda.com/download](https://www.anaconda.com/download)
- **Miniconda**（精简版，约400MB，推荐）: [https://docs.conda.io/en/latest/miniconda.html](https://docs.conda.io/en/latest/miniconda.html)

下载对应Windows版本的安装包，双击安装。安装时勾选 "Add Anaconda to my PATH environment variable"。

#### 2. 打开Anaconda Prompt

从开始菜单找到并打开 **Anaconda Prompt** 或 **Anaconda Powershell Prompt**。

#### 3. 创建虚拟环境

```bash
# 创建名为 autoencoder_env 的Python 3.9环境
conda create -n autoencoder_env python=3.9 -y
```

#### 4. 激活环境

```bash
conda activate autoencoder_env
```

激活后，命令行前面会显示 `(autoencoder_env)`。

#### 5. 进入项目目录

```bash
# 将下面的路径替换为您的实际项目路径
cd "D:\日常事务\李老师事务\2025编书系统工程与管理工程书\2025.09.07\所有程序\自编码器（Autoencoder）用于谱图降维及特征重构的特点"
```

#### 6. 安装依赖

```bash
pip install -r requirements.txt
```

安装过程大约需要5-10分钟，取决于网络速度。

#### 7. 验证安装

```bash
python -c "import torch; import sklearn; import numpy; print('环境配置成功！')"
```

如果显示 "环境配置成功！"，说明环境已正确配置。

---

### 方法二：使用系统Python

如果您不想使用Conda，可以直接使用系统Python。

#### 1. 检查Python版本

打开 **PowerShell** 或 **命令提示符**，输入：

```bash
python --version
```

确保显示的版本是 3.8、3.9 或 3.10。如果没有安装Python，请前往 [https://www.python.org/downloads/](https://www.python.org/downloads/) 下载安装。

#### 2. 创建虚拟环境（可选但推荐）

```bash
# 进入项目目录
cd "D:\日常事务\李老师事务\2025编书系统工程与管理工程书\2025.09.07\所有程序\自编码器（Autoencoder）用于谱图降维及特征重构的特点"

# 创建虚拟环境
python -m venv venv
```

#### 3. 激活虚拟环境

```bash
# PowerShell
.\venv\Scripts\Activate.ps1

# 或 命令提示符
.\venv\Scripts\activate.bat
```

激活后，命令行前面会显示 `(venv)`。

**注意**: 如果PowerShell报错 "无法加载文件，因为在此系统上禁止运行脚本"，请以管理员身份运行PowerShell并执行：

```bash
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

#### 4. 升级pip

```bash
python -m pip install --upgrade pip
```

#### 5. 安装依赖

```bash
pip install -r requirements.txt
```

#### 6. 验证安装

```bash
python -c "import torch; import sklearn; import numpy; print('环境配置成功！')"
```

---

## 项目使用

### 1. 快速体验

运行快速开始脚本，了解项目的基本功能：

```bash
python quick_start.py
```

**输出内容**:
- 生成40个模拟NIR光谱样本（8种农产品）
- 创建Autoencoder模型
- 演示编码-解码过程
- 运行10个epoch的简单训练

**预期时间**: 约30秒-1分钟

### 2. 数据生成（可选）

如果需要重新生成数据：

```python
# 在Python交互环境中
from data import load_or_generate_data
import config

# regenerate=True 表示强制重新生成
spectra, labels, origins = load_or_generate_data(
    config.DATA_DIR,
    samples_per_product=50,
    regenerate=True
)
```

### 3. 开始训练

启动完整的50轮训练：

```bash
python train.py
```

**训练过程**:
- 将400个样本划分为训练/验证/测试集（70%/15%/15%）
- 训练50个epoch（带早停）
- 自动保存最佳模型
- 生成训练曲线

**预期时间**:
- **CPU模式**: 约5-10分钟
- **GPU模式**: 约2-5分钟（如果有兼容GPU）

**输出文件**:
- `saved_models/autoencoder_best.pth` (最佳模型)
- `results/training_results.json` (训练结果)
- `results/training_curves.png` (训练曲线图)

### 4. 降维应用

#### 批量降维（推荐）

将所有光谱压缩到100维潜在特征：

```bash
python dimension_reduction.py --mode batch
```

**输出**:
- `results/latent_features.npy` (100维潜在特征)
- `results/latent_labels.npy` (对应的标签)
- 控制台显示降维统计信息

#### 单个样本降维

查看几个样本的降维示例：

```bash
python dimension_reduction.py --mode single
```

### 5. 重构应用

#### 单个样本重构

查看原始光谱与重构光谱的对比：

```bash
python reconstruct.py --mode single
```

**输出**:
- 5个样本的重构对比图
- 每个样本的MSE、MAE、相关系数

#### 批量重构评估

评估模型在测试集上的重构性能：

```bash
python reconstruct.py --mode batch
```

**输出**:
- 测试集整体重构指标（MSE、RMSE、MAE、PSNR、Correlation）

#### 从潜在特征重构

从保存的潜在特征重构光谱：

```bash
python reconstruct.py --mode latent
```

**注意**: 需要先运行 `dimension_reduction.py --mode batch` 生成潜在特征。

### 6. 可视化

生成所有可视化图表：

```bash
python visualize.py
```

**输出图表**:
1. `latent_space_tsne.png` - t-SNE潜在空间投影
2. `latent_space_pca.png` - PCA潜在空间投影
3. `latent_distribution.png` - 潜在特征分布
4. `reconstruction_samples.png` - 重构样本对比

**预期时间**: 约1-2分钟（t-SNE计算较慢）

---

## 配置说明

所有配置项都在 `config.py` 文件中。以下是常用配置：

### 1. 设备配置

```python
# config.py

# 设备模式
DEVICE_MODE = 'cpu'  # 选项: 'cpu', 'cuda', 'auto'
```

- `'cpu'`: 强制使用CPU（默认，兼容性最好）
- `'cuda'`: 强制使用GPU（需要CUDA兼容的GPU）
- `'auto'`: 自动检测（有GPU则用GPU，无则用CPU）

**修改方法**:
1. 用文本编辑器打开 `config.py`
2. 找到 `DEVICE_MODE = 'cpu'` 这行
3. 修改为 `DEVICE_MODE = 'auto'` 或 `'cuda'`
4. 保存文件

### 2. 训练参数

```python
# config.py

BATCH_SIZE = 32       # 批次大小（显存不足可减小）
EPOCHS = 50           # 最大训练轮数
LEARNING_RATE = 0.001 # 学习率
LATENT_DIM = 100      # 潜在空间维度
ACTIVATION = 'SELU'   # 激活函数
```

### 3. 数据配置

```python
# config.py

INPUT_DIM = 737       # 光谱维度
SAMPLES_PER_PRODUCT = 50  # 每种农产品的样本数
WAVENUMBER_RANGE = (9100, 3900)  # 波数范围 (cm⁻¹)
```

### 4. 可视化配置

```python
# config.py

# t-SNE配置
TSNE_CONFIG = {
    'n_components': 2,
    'perplexity': 30,
    'n_iter': 1000,
    'random_state': 42,
}
```

---

## 常见问题

### Q1: 训练速度很慢怎么办？

**A**: 
- **使用GPU加速**: 如果有兼容的NVIDIA GPU，修改 `config.py` 中的 `DEVICE_MODE = 'auto'`
- **减少样本数**: 修改 `SAMPLES_PER_PRODUCT = 25`（从50改为25）
- **减少训练轮数**: 修改 `EPOCHS = 30`
- **增大批次**: 修改 `BATCH_SIZE = 64`（如果内存充足）

### Q2: 显存/内存不足怎么办？

**A**:
- **减小批次大小**: 修改 `BATCH_SIZE = 16` 或 `8`
- **使用CPU**: 修改 `DEVICE_MODE = 'cpu'`
- **减少样本数**: 修改 `SAMPLES_PER_PRODUCT = 25`

### Q3: 如何调整压缩比？

**A**:
修改潜在维度 `LATENT_DIM`：
- 更高压缩：`LATENT_DIM = 50`（压缩率14.74x）
- 更低压缩：`LATENT_DIM = 200`（压缩率3.69x）

注意：压缩率越高，重构误差可能越大。

### Q4: 如何使用自己的NIR光谱数据？

**A**:
1. 准备数据：将光谱数据保存为numpy数组 `(N, 737)` 格式
2. 保存为 `data/spectra.npy`
3. 如果有标签，保存为 `data/labels.npy`
4. 修改 `config.py` 中的相关参数（如果维度不同）
5. 运行 `python train.py`

### Q5: 重构效果不理想怎么办？

**A**:
- **增加训练轮数**: 修改 `EPOCHS = 100`
- **调整学习率**: 尝试 `LEARNING_RATE = 0.0005` 或 `0.002`
- **增加潜在维度**: 修改 `LATENT_DIM = 150`
- **尝试其他激活**: 修改 `ACTIVATION = 'ReLU'` 或 `'Tanh'`

### Q6: t-SNE可视化很慢怎么办？

**A**:
- 减少迭代次数：修改 `TSNE_CONFIG['n_iter'] = 500`
- 只用PCA可视化（速度快）
- 减少样本数进行可视化

---

## 错误排查

### 错误1: `ModuleNotFoundError: No module named 'xxx'`

**原因**: 依赖包未安装

**解决**:
```bash
pip install -r requirements.txt
```

或单独安装缺失的包：
```bash
pip install torch numpy scikit-learn matplotlib tqdm
```

### 错误2: `CUDA error: no kernel image is available`

**原因**: PyTorch版本与GPU不兼容

**解决**:
- **方案1**: 使用CPU模式（最简单）
  ```python
  # config.py
  DEVICE_MODE = 'cpu'
  ```

- **方案2**: 安装兼容的PyTorch版本
  访问 [https://pytorch.org/](https://pytorch.org/) 选择对应CUDA版本

### 错误3: `FileNotFoundError: 模型文件不存在`

**原因**: 模型文件不存在

**解决**:
- 先运行 `python train.py` 训练模型
- 确保训练成功完成，`saved_models/` 文件夹中有 `.pth` 文件

### 错误4: `ValueError: 不支持的激活函数`

**原因**: 配置的激活函数不支持

**解决**:
- 检查 `config.py` 中的 `ACTIVATION` 设置
- 支持的选项: `'SELU'`, `'ReLU'`, `'Tanh'`

### 错误5: 可视化时出现 `RuntimeError: No artists with labels found`

**原因**: 数据为空或标签缺失

**解决**:
- 先运行 `python train.py` 确保有训练好的模型
- 先运行 `python dimension_reduction.py --mode batch` 生成潜在特征

### 错误6: 训练时出现 `nan` 损失

**原因**: 学习率过大或数据有问题

**解决**:
- 降低学习率：`LEARNING_RATE = 0.0001`
- 检查数据是否有异常值（inf、nan）
- 尝试其他激活函数

---

## 技术支持

如果遇到本指南未覆盖的问题：

1. **检查Python版本**: 确保使用Python 3.8-3.10
2. **查看完整错误信息**: 复制完整的错误堆栈信息
3. **检查文件路径**: 确保所有文件路径正确，避免中文路径问题
4. **重新安装环境**: 删除虚拟环境，重新按照本指南安装

---

## 附录：完整工作流程

以下是从零开始到获得结果的完整流程：

```bash
# 1. 创建并激活Conda环境
conda create -n autoencoder_env python=3.9 -y
conda activate autoencoder_env

# 2. 进入项目目录
cd "你的项目路径"

# 3. 安装依赖
pip install -r requirements.txt

# 4. 快速体验
python quick_start.py

# 5. 训练模型（5-10分钟）
python train.py

# 6. 降维
python dimension_reduction.py --mode batch

# 7. 重构
python reconstruct.py --mode single

# 8. 可视化（1-2分钟）
python visualize.py

# 完成！
```

---

**祝您使用愉快！如有任何问题，请参考本指南的常见问题和错误排查部分。**

