# 环境配置指南

> 本指南帮助您从零开始配置Autoencoder NIR光谱降维系统的运行环境

## 📋 目录

- [系统要求](#系统要求)
- [方式一：使用Miniconda（推荐）](#方式一使用miniconda推荐)
- [方式二：使用系统Python](#方式二使用系统python)
- [验证安装](#验证安装)
- [常见问题](#常见问题)

---

## 系统要求

### 硬件要求

- **内存**: 建议4GB以上
- **硬盘**: 至少300MB可用空间（包含环境和依赖）
- **GPU**: 可选（默认使用CPU，兼容性最佳）
- **处理器**: 任意现代处理器

### 软件要求

- **操作系统**: Windows 10/11、Linux、macOS
- **Python版本**: 3.8 - 3.10
- **网络连接**: 需要网络下载依赖包

---

## 方式一：使用Miniconda（推荐）

Conda可以创建独立的Python环境，避免与系统其他项目冲突。

### 步骤1：安装Miniconda

**下载地址**：
- 清华镜像：https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/
- 官方地址：https://docs.conda.io/en/latest/miniconda.html

**选择版本**：
- Windows: `Miniconda3-latest-Windows-x86_64.exe`
- Linux: `Miniconda3-latest-Linux-x86_64.sh`
- macOS: `Miniconda3-latest-MacOSX-x86_64.sh`

**安装说明**：
- Windows: 双击安装包，建议勾选"Add Anaconda to my PATH environment variable"
- Linux/macOS: 在终端运行 `bash Miniconda3-latest-*.sh`

### 步骤2：创建虚拟环境

```bash
# 创建名为autoencoder_env的Python 3.9环境
conda create -n autoencoder_env python=3.9 -y
```

### 步骤3：激活环境

```bash
conda activate autoencoder_env
```

激活后，命令行前面会显示 `(autoencoder_env)`。

### 步骤4：配置镜像源（可选，加速下载）

```bash
# 添加清华镜像
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/

# 配置pip镜像
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

### 步骤5：进入项目目录

```bash
# 将下面的路径替换为您的实际项目路径
cd "D:\...\自编码器（Autoencoder）用于谱图降维及特征重构"
```

### 步骤6：安装依赖

```bash
# 使用conda安装主要依赖（推荐）
conda install pytorch numpy scikit-learn matplotlib tqdm -y

# 或使用pip安装所有依赖
pip install -r requirements.txt
```

**安装时间**: 约5-10分钟，取决于网络速度

**特殊说明**：
- 如果需要GPU加速，请访问 [PyTorch官网](https://pytorch.org/) 选择对应CUDA版本的安装命令
- 本项目默认使用CPU模式，无需GPU也能正常运行

---

## 方式二：使用系统Python

如果您不想使用Conda，可以直接使用系统Python。

### 步骤1：检查Python版本

打开 **PowerShell**（Windows）或 **终端**（Linux/macOS），输入：

```bash
python --version
```

确保显示的版本是 3.8、3.9 或 3.10。

**如果没有安装Python**：
- Windows: 访问 https://www.python.org/downloads/ 下载安装
- Linux: `sudo apt install python3.9` 或 `sudo yum install python39`
- macOS: 使用Homebrew `brew install python@3.9`

### 步骤2：创建虚拟环境（推荐）

```bash
# 进入项目目录
cd "你的项目路径"

# 创建虚拟环境
python -m venv venv
```

### 步骤3：激活环境

**Windows PowerShell**:
```bash
.\venv\Scripts\Activate.ps1
```

**Windows 命令提示符**:
```bash
.\venv\Scripts\activate.bat
```

**Linux/macOS**:
```bash
source venv/bin/activate
```

激活后，命令行前面会显示 `(venv)`。

**PowerShell执行策略问题**：
如果PowerShell报错"无法加载文件，因为在此系统上禁止运行脚本"，以管理员身份运行：
```bash
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 步骤4：升级pip

```bash
python -m pip install --upgrade pip
```

### 步骤5：安装依赖

```bash
pip install -r requirements.txt
```

---

## 验证安装

运行快速测试程序，验证环境配置是否成功：

```bash
python quick_start.py
```

**预期输出**：
```
配置加载完成，使用设备: cpu
💡 提示: 如果您有兼容的GPU，可以在config.py中修改 DEVICE_MODE = 'auto' 来启用GPU加速

=== Autoencoder NIR光谱降维 - 快速开始演示 ===

步骤1: 生成模拟NIR光谱数据
已生成 40 个样本，8 种农产品
光谱维度: 737 (9100.0 - 3900.0 cm⁻¹)
...
```

### 验证关键库

```bash
# 验证PyTorch
python -c "import torch; print('PyTorch版本:', torch.__version__)"

# 验证NumPy
python -c "import numpy; print('NumPy版本:', numpy.__version__)"

# 验证scikit-learn
python -c "import sklearn; print('scikit-learn版本:', sklearn.__version__)"

# 验证CUDA（如果使用GPU）
python -c "import torch; print('CUDA可用:', torch.cuda.is_available())"
```

**预期输出**：
```
PyTorch版本: 1.10.0 或更高
NumPy版本: 1.21.0 或更高
scikit-learn版本: 1.0.0 或更高
CUDA可用: True 或 False
```

---

## 常见问题

### Q1: pip安装速度很慢怎么办？

**现象**：
```
Downloading... (进度很慢)
```

**解决方案**：
```bash
# 使用清华镜像源加速
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或配置为默认镜像
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q2: conda命令找不到？

**现象**：
```
'conda' 不是内部或外部命令
```

**解决方案**：
1. 确认已正确安装Miniconda/Anaconda
2. 使用"Anaconda Prompt"而不是普通命令提示符
3. 或将Anaconda添加到系统PATH环境变量

### Q3: PyTorch安装失败？

**现象**：
```
ERROR: Could not find a version that satisfies the requirement torch
```

**解决方案**：
```bash
# 使用conda安装（更可靠）
conda install pytorch -c pytorch -y

# 或指定CPU版本
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
```

### Q4: 虚拟环境激活失败（Windows PowerShell）？

**现象**：
```
无法加载文件 venv\Scripts\Activate.ps1，因为在此系统上禁止运行脚本
```

**解决方案**：
以管理员身份运行PowerShell并执行：
```bash
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q5: 提示缺少某个模块？

**现象**：
```
ModuleNotFoundError: No module named 'xxx'
```

**解决方案**：
```bash
# 重新安装所有依赖
pip install -r requirements.txt

# 或单独安装缺失的模块
pip install xxx
```

### Q6: GPU相关错误？

**现象**：
```
CUDA error: no kernel image is available
```

**解决方案**：
1. **最简单的方法**：使用CPU模式（默认已配置）
   ```python
   # config.py中已设置
   DEVICE_MODE = 'cpu'
   ```

2. **如需GPU加速**：访问 https://pytorch.org/ 安装兼容版本的PyTorch

---

## 环境管理

### 常用命令

```bash
# 激活环境
conda activate autoencoder_env

# 退出环境
conda deactivate

# 查看已安装的包
conda list
pip list

# 删除环境（如需重新配置）
conda remove -n autoencoder_env --all

# 更新某个包
pip install --upgrade package_name
```

### 导出环境配置

```bash
# 导出conda环境
conda env export > environment.yml

# 导出pip依赖
pip freeze > requirements.txt
```

### 在其他机器上复现环境

```bash
# 使用conda配置文件
conda env create -f environment.yml

# 使用pip配置文件
pip install -r requirements.txt
```

---

## 下一步

环境配置完成后，请阅读以下文档：

- 📖 **[README.md](README.md)** - 项目简介和快速开始
- 📖 **[使用指南.md](使用指南.md)** - 详细的功能使用说明

---

## 技术支持

如果遇到本指南未覆盖的问题：

1. **检查Python版本**: 确保使用Python 3.8-3.10
2. **查看完整错误信息**: 复制完整的错误堆栈信息
3. **检查文件路径**: 确保路径正确，避免包含特殊字符
4. **重新安装环境**: 删除虚拟环境，按照本指南重新安装

---

**祝您配置顺利！** 🎉

